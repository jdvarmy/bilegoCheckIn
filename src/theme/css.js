const baseSize = 8;

export default {
  sizes: {
    base: `${baseSize}px`,
    xs:   `${baseSize / 2}px`,
    md:   `${baseSize * 2}px`,
    lg:   `${baseSize * 3}px`, // 24
    xl:   `${baseSize * 4}px`, // 32
    xxl:  `${baseSize * 5}px`,
    xxxl: `${baseSize * 6}px`,
  }
};
