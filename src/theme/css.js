const baseSize = 8, borderRadius = 4;

export default {
  sizes: {
    borderRadius: borderRadius,
    base: baseSize,
    xs:   baseSize / 2,
    md:   baseSize * 2,
    lg:   baseSize * 3, // 24
    xl:   baseSize * 4, // 32
    xxl:  baseSize * 5,
    xxxl: baseSize * 6,
  }
};
